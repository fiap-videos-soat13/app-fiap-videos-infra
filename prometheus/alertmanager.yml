route:
  receiver: default
  group_by: ['alertname', 'job']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h

receivers:
  - name: default
    webhook_configs:
      - url: 'http://127.0.0.1:1/dummy'
        send_resolved: true
