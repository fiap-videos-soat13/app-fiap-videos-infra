apiVersion: v1
kind: ConfigMap
metadata:
  name: fiap-videos-storage
data:
  STORAGE_BACKEND: minio
  S3_BUCKET: fiap-videos
  S3_ENDPOINT: http://minio:9000
  AWS_ACCESS_KEY_ID: minioadmin
  AWS_SECRET_ACCESS_KEY: minioadmin
  AWS_REGION: us-east-1
