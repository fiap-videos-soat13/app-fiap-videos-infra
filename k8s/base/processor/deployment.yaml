apiVersion: apps/v1
kind: Deployment
metadata:
  name: fiap-videos-processor
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fiap-videos-processor
  template:
    metadata:
      labels:
        app: fiap-videos-processor
    spec:
      containers:
        - name: processor
          image: fiap-videos-processor:latest
          ports:
            - containerPort: 3001
          envFrom:
            - secretRef:
                name: fiap-videos-processor-secrets
            - configMapRef:
                name: fiap-videos-storage
            - configMapRef:
                name: fiap-videos-messaging
            - configMapRef:
                name: fiap-videos-processor-runtime
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: '2'
              memory: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: fiap-videos-processor
spec:
  selector:
    app: fiap-videos-processor
  ports:
    - port: 3001
      targetPort: 3001
